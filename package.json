{
  "name": "discord-break-reminder",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "secret": "wrangler secret put DISCORD_WEBHOOK_URLS",
    "build:admin-ui": "node scripts/build-admin-ui.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
